const $=(s,r=document)=>r.querySelector(s);
async function api(u,o={}){const r=await fetch(u,{credentials:"include",...o,headers:{...(o.body?{"Content-Type":"application/json"}:{}),...(o.headers||{})}});let d;try{d=await r.json()}catch{throw Error("Server returned "+r.status)}if(!r.ok||d.success===false)throw Error(d.error||"Request failed");return d}
async function init(){try{const d=await api("/api/me");if(!d.user)throw 0;if(String(d.user.role).toLowerCase()!=="admin"){location.replace("/dashboard.html");return}const o=await api("/api/admin/overview");const s=o.stats||o;for(const[k,id]of Object.entries({users:"#totalUsers",active_users:"#activeUsers",premium_users:"#premiumUsers",license_keys:"#licenseKeys",active_keys:"#activeKeys"})){const e=$(id);if(e)e.textContent=s[k]??0}const u=await api("/api/admin/users");const body=$("#usersBody");if(body)(u.users||[]).forEach(x=>{const tr=document.createElement("tr");tr.innerHTML=`<td>${esc(x.id)}</td><td>${esc(x.username)}</td><td>${esc(x.email)}</td><td>${esc(x.role)}</td><td>${esc(x.plan||"free")}</td><td>${esc(x.status)}</td>`;body.appendChild(tr)})}catch{location.replace("/login.html")}}
function esc(v){return String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]) )}
document.addEventListener("DOMContentLoaded",init)
