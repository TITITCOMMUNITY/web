BILSX clean frontend baseline.
Replace public/app.js, login.html, register.html, dashboard.html and admin.html.
The included style.css is a minimal fallback; keep your existing style.css if you want the current visual design.
Keep existing login.js and logout.js initially. Replace API files only after verifying the frontend.
Do not encrypt/obfuscate until authentication, session, D1 and authorization are stable.
