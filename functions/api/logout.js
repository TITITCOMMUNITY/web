export async function onRequestPost({request,env}){const c=(request.headers.get('Cookie')||'').split(';').map(x=>x.trim()).find(x=>x.startsWith('bilsx_session='));if(c){const t=c.slice('bilsx_session='.length),h=await sha(t);await env.DB.prepare('DELETE FROM sessions WHERE token_hash=?1').bind(h).run()}return new Response('{"success":true}',{headers:{'Content-Type':'application/json','Set-Cookie':'bilsx_session=; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age=0'}})}async function sha(v){const d=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(v));return [...new Uint8Array(d)].map(x=>x.toString(16).padStart(2,'0')).join('')}
